# WASA Chat 測定結果ログ

この文書は、設計判断の根拠になった実測値を、測った順に記録したものです。

他の文書と違って、ここは**読み物ではなく台帳**です。上から順に読む必要はありません。
「なぜこの数字になっているんだろう」と思ったときに、該当する測定（M1、M2…）を
探して読む使い方を想定しています。

ここには2つだけルールがあります。

1. **測っていないことは書かない。** 推測や見立ては[01-設計方針.md](01-設計方針.md)側に置きます
2. **数字が動いたら、まず測定器を疑う。** 実際、精度が変わったと思ったら
   評価スクリプト側の欠陥だった、ということが何度もありました

新しい測定を足すときの書き方は、いちばん下の「測定の型」にあります。

---

## M1. 検索ベースライン（BM25 / 文字bigram）

- 日付: 2026-08-06
- 対象: チャンク348件、ゴールデンデータ31問（うち検索採点対象29問）
- 実行: `python eval/retrieval_eval.py`
- 検索器: 文字bigramのBM25（形態素解析器なし、外部依存なし）

| k | Evidence Recall | All-Evidence Recall | Page Recall |
|---:|---:|---:|---:|
| 1 | 24.1% | 20.7% | 31.0% |
| 3 | 41.4% | 37.9% | 69.0% |
| 5 | **55.2%** | 41.4% | **72.4%** |
| 10 | 69.0% | 55.2% | 82.8% |

MRR: **0.390** ／ 無情報ベースライン（インデックス順）: Recall@5 = 6.9%, MRR = 0.080

### 種別ごとの Hit@5

| 種別 | 結果 |
|---|---|
| procedure | 5/7 |
| fact | 7/11 |
| partial | 3/5 |
| global | 1/2 |
| comparison | **0/2** |
| false_premise | **0/1** |
| unanswerable | **0/1** |

### 読み取れたこと

**1. ページ単位のRecallは実力を17ポイント過大評価する。**

`Page Recall@5 = 72.4%` に対し `Evidence Recall@5 = 55.2%`。設計方針 §5-3 で
「36,261字ある『駆動・フレーム班』がヒットしても正解の節が取れているとは限らない」
と書いた懸念が、そのまま17ポイントの差として出た。**ページ単位で測っていたら
実力を見誤っていた。** ゴールデンデータをチャンク単位で作った判断は正しかった。

**2. 語彙検索が原理的に弱い領域が、そのまま0%として出た。**

comparison（2問）、false_premise（1問）、unanswerable（1問）がすべて Hit@5 = 0。

- q18「早稲田大学の滑空機はどうやって作るのですか？」→ 正解の順位 **98位**。
  前提が誤っている質問は、質問文の語彙が正解チャンクと重ならないため原理的に引けない
- q10「41stと40thの空力設計の違い」→ **24位**。比較質問は2つの対象を1クエリで
  表すため、どちらのチャンクとも中途半端にしかマッチしない
- q29「40代はどういう活動をしたのか」→ **43位**。全域型も同様

これらは検索器のチューニングでは埋まらない。**クエリ書き換え（比較質問を2本に割る）
や、メタデータフィルタ（代=40で絞る）といった別の層が要る**という根拠になった。

**3. k=5 では足りず、k=10 で69%。**

Recall@10 が Recall@5 を14ポイント上回る。候補を広く取ってリランキングする構成
（設計方針 §6-4）に合理性がある。

### この数字の位置づけ

これは**検索層のみ**の性能であり、Phase 1 で作る「目次方式（L1）」とは
比較対象が違う。目次方式はClaudeが目次を見てページを選ぶため、
上表の語彙一致とは別の経路で正解に到達しうる。**両者を同じ設問で測って
初めて、検索層（Phase 2）が必要かどうかが判断できる。**

次の測定 M2 は「目次方式のみ、検索層なし」。Anthropic APIキーが必要。

---

## M2a. 目次方式によるページ選択（検索層なし）

- 日付: 2026-08-06
- 対象: 目次14,743字（10,288トークン）、ゴールデンデータ31問（ページ採点対象29問）
- 実行: `python eval/toc_eval.py`
- モデル: **qwen3:30b-a3b（ローカル / Ollama）**。非公開Wikiのデータを外部に出さないため

| 指標 | 結果 |
|---|---|
| **Page Recall@3** | **93.1%** (27/29) |
| All-Pages Recall@3 | 72.4% (21/29) |
| 1位が正解 | 82.8% (24/29) |
| 回答可否の判定 | 74.2% (23/31) |
| 平均所要 | 3.9秒/問（合計122秒） |

### BM25ベースライン（M1）との比較 ※ページ単位で粒度を揃えてある

| 手法 | Page Recall |
|---|---|
| BM25 @3 | 69.0% |
| BM25 @5 | 72.4% |
| **目次方式 @3** | **93.1%** |

**目次方式は BM25 を24ポイント上回った。** BM25の@5（72.4%）すら21ポイント上回っている。

### 種別ごとの Hit（BM25との対比）

| 種別 | BM25 Hit@5 | 目次方式 Hit@3 |
|---|---|---|
| fact | 7/11 | **11/11** |
| procedure | 5/7 | **7/7** |
| comparison | **0/2** | **2/2** |
| global | 1/2 | **2/2** |
| partial | 3/5 | 4/5 |
| unanswerable | 0/1 | 1/1 |
| false_premise | 0/1 | **0/1** |

### 読み取れたこと

**1. 語彙検索が原理的に弱かった領域を、目次方式が埋めた。**

M1 で「comparison 0/2、global 1/2 は検索器のチューニングでは埋まらない」と書いた。
目次方式ではそれぞれ **2/2、2/2**。q10（41st と 40th の比較）はBM25で24位だったが、
目次方式は両方のページを正しく選んでいる。**LLMが目次全体を見て判断する経路は、
語彙一致とは別物として機能している。**

**2. Phase 2（検索層）の優先度は下がった。**

93.1%という数字は、検索層を足す前の Phase 1 単体のもの。**まず検索層なしで進め、
不足が具体化してから足す**という §2 の方針が、数字で裏付けられた。

**3. ハルシネーション対策が必須。存在しないページ名を8件返した。**

| 質問 | 返した架空のタイトル | 正体 |
|---|---|---|
| q31 | 「空力」「構造」「翼」 | **班名**をページ名として返した |
| q20 | 「鳥コンまでの流れ」「鳥コンスケジュール」 | **節名**をページ名として返した |
| q23 | 「構造設計 41st」 | 命名規則から**推測して生成**した（実ページは未作成） |
| q18 | 「WASA Wiki 目次」「質問: 早稲田大学の…」 | プロンプトの一部をそのまま返した |

→ **返ってきたタイトルは必ず目次と照合し、存在しないものは落とす。** 実装必須。

**4. 渡す文脈量が大きすぎる。ページ→チャンクの2段目が要る。**

選択した3ページの合計文字数は中央値9,770字だが、**最大48,100字**。
36,261字ある「駆動・フレーム班」を丸ごと渡すと文脈を圧迫する。
ページを選んだ後、そのページ内でチャンクを絞る2段目が必要。

**5. false_premise は両手法とも 0/1。**

q18「早稲田大学の滑空機はどうやって作るのですか？」は、BM25では98位、
目次方式ではプロンプトの一部をタイトルとして返して破綻した。
**前提が誤った質問は、検索でも目次選択でも拾えない。** 回答生成側で
「そもそもWASAは滑空機部門ではない」と気づかせる必要がある。

**6. リード文が空のページが5件ある。**

`メインページ / 役職決め / 合宿 / 駆動設計 / 電装39代設計コンセプト・反省`。
リード文が空だと目次の情報量が落ちる。q15（フライトシミュレーター）を外した一因と見られる
（メインページの節一覧には「フライトシミュレーター」が入っているが、リード文が空で、
語彙的に「フライトシミュレーターの開発もしている」と書かれた人物ページに負けた）。

### この数字の限界

**ローカルモデル（qwen3:30b-a3b）での測定である。** Claude で測り直せば数字は変わる。
ただし本測定の目的は**アーキテクチャの妥当性判断**であり、その点では十分に結論が出た。
回答品質（Faithfulness等）の測定 M2b は、本番モデルで別途行う必要がある。

---

## M2b. エンドツーエンドの回答品質

- 日付: 2026-08-07
- 対象: ゴールデンデータ31問
- 実行: `python eval/answer_eval.py`
- モデル: qwen3:30b-a3b（ローカル）。LLM呼び出し100回 / 合計1,753秒

### 結論を先に: **回答品質は測れなかった。アーキテクチャの検証はできた。**

ローカルモデルが「思考を書かない」「日本語で答える」という指示に安定して従わず、
回答本文に思考過程が混入した。`think:false`（API）、`/no_think`（qwen3のソフトスイッチ）、
プロンプトでの明示の3つを試したがいずれも効かなかった。したがって
**出典明示率とFaithfulnessの数字は、モデルの癖に汚染されており指標にならない。**

### アーキテクチャとして検証できたこと

| 項目 | 結果 | 判定 |
|---|---|---|
| ページ選択の的中 | 27/29 | ✅ M2aと同水準を再現 |
| 文脈量 | 中央値6,998字 / **最大12,645字** | ✅ M2aの48,100字から圧縮できた |
| 架空ページ名の除去 | **12件を捕捉** | ✅ ガードが機能 |
| 回答不能な問いで「記載なし」 | 6/8 | ✅ 機能している |

**2段絞り込みの効果が数字で出た。** M2a では選択3ページの合計が最大48,100字だったが、
パンくずだけを見せて節を選ばせる段を挟んだ結果、最大12,645字に収まった。

**ガードが捕捉した架空ページ名の内訳が示唆的である。**

| 種類 | 例 |
|---|---|
| 節名をページ名として返した | `作業場ルール` `パワメとは` `各種申請の流れについて` `鳥コン資料` |
| 推測で生成した | `42代駆動メモ` `HPA交流会関する引き継ぎ資料` |
| 班名 | `構造` `翼` |
| 空文字列 | `""` ×3（q15） |

ガードが無ければ、存在しないページを根拠として回答していた。**M2aで見つけた
課題への対処が、実際に効いていることが確認できた。**

### 測れなかったこと、およびその原因

**1. 出典明示 17/31 (54.8%)** — プロンプトで「出典: ページ名（最終更新: YYYY-MM）の形で
必ず挙げる」と指示しているが従わない。長い資料の後ろに指示を置くと効きにくい。

→ **アプリ側で構造的に解決する。** 回答本文にLLMが出典を書くことに頼らず、
SSEの `pages` イベントで送っている出典をUIが別枠で表示する（実装済み）。
**LLMの指示追従に依存しない設計に寄せる**という判断。

**2. Faithfulness 61.3%** — 判定器が機能していない。判定理由の欄に
「資料に書かれていない事実を述べていたら faithful = false」という
**判定基準の文をそのままオウム返し**していた。

→ スキーマの順序を変え、`unsupported_claim`（裏付けの無い記述の引用）を
先に書かせてから `faithful` を決める形に修正した。判定を先に置くと、
根拠を考える前に決めてしまう。

**3. 答えられる問いで誤って「記載がない」5件** — うち q07 は実際には正しく
作業場ルールを列挙しており、**検出用の正規表現の誤検知**だった。
残りは思考文の混入によるもの。

### 判断を変えた点

- **回答品質の測定は本番モデル（Claude）が要る。** ローカルモデルでの代替は
  アーキテクチャ検証までが限界。docs/01 §9 の M2b は「APIキー入手後に再実行」とする
- **出典の提示はLLMに依存しない。** UIが構造的に出す（実装済み）
- **判定器の検証は必須。** 「暫定値として扱う」では不十分で、
  実際に機能しているかを最初に確かめる必要がある

---

## M2b-2. Gemini（無料枠）での再測定

- 日付: 2026-08-07
- モデル: **gemini-flash-lite-latest**（Google Gemini 無料枠）
- 実行: `LLM_PROVIDER=gemini python eval/answer_eval.py`

無料枠は送信内容が学習に使われうるが、そのリスクを承知のうえでの選択。
個人情報（メール・生年月日・電話番号）はマスク済み。

### ローカルモデルとの比較

| 指標 | qwen3:30b-a3b（ローカル） | gemini-flash-lite |
|---|---|---|
| ページ選択の的中 | **27/29 (93.1%)** | 24/29 (82.8%) |
| **出典の明示** | 17/31 (54.8%) | **31/31 (100%)** |
| **架空ページ名** | **12件** | **0件** |
| Faithfulness（判定器） | 61.3%（判定器が機能せず） | 80.6% |
| 所要 | 1,753秒 | **128秒** |

**M2bで「測れなかった」ものが測れるようになった。**

- **出典の明示が54.8% → 100%。** ローカルモデルが従わなかった
  「出典を必ず挙げる」という指示に、Geminiは全問で従った
- **架空ページ名が12件 → 0件。** 節名や班名をページ名として返す挙動が消えた
- **判定器が機能し始めた。** ローカルでは判定基準の文をオウム返ししていたが、
  Geminiは具体的な記述を引用してくる（例: q03で「2年以上経過している」という
  記述を、最終更新2026-04-28に対する誤りとして正しく指摘した）
- **13.7倍速い。** 128秒 vs 1,753秒

一方で**ページ選択はローカルの30Bモデルに10ポイント負けた**（93.1% → 82.8%）。
flash-lite は最小構成のモデルであり、指示追従は強いが判断力では劣る。
**指示追従と判断力は別の軸である**ことが数字で見えた。

### 自分の評価スクリプトの欠陥を2つ見つけた

**1. `partial` 型と `unanswerable` 型を同じ物差しで測っていた。**

回答本文に「記載がありません」が一度でも出れば「拒否した」と数えていた。
しかしプロンプトは「**何が書かれていて何が書かれていないかを分けて述べる**」と
指示している。つまり**指示どおりに答えた回答ほど誤検知される**構造だった。

種別ごとに基準を分けて再採点した結果:

| 種別 | 適切な回答の出し方 | 結果 |
|---|---|---|
| comparison | 2/2 | |
| false_premise | 1/1 | q18を正解。ローカルは0/1 |
| procedure | 6/7 | |
| fact | 8/11 | |
| partial | 2/5 | 欠落を認めつつ中身も出す、が難しい |
| unanswerable | 1/2 | |
| global | 1/3 | |
| **合計** | **21/31 (67.7%)** | |

**2. 「答えられるのに拒否」9件のうち、実際の拒否は6件だった。**
残り3件（q21, q27, q28）は、指示どおり「書かれていること／いないこと」を
分けて答えた**正しい回答**だった。

### 読み取れたこと

**指示追従は小さいモデルでも十分だが、判断力は別。**
ページ選択（どのページを読むべきかの判断）は30Bのローカルモデルが勝ち、
出力形式の遵守（出典・実在するページ名・日本語）はflash-liteが圧勝した。
**段によって求められる能力が違う**ので、本番では段ごとにモデルを変える設計が
合理的かもしれない（選択は賢いモデル、整形は安いモデル）。

**苦手な種別が明確になった。** `partial`（2/5）と `global`（1/3）。
「書かれていないことを、書かれていないと正しく言う」のが最も難しい。
これは docs/01 §5-3 で Faithfulness を最重要指標に置いた判断と整合する。

### 無料枠の制約（実測）

| モデル | 状況 |
|---|---|
| gemini-3.6-flash | **日次20リクエスト**。31問の測定に届かない |
| gemini-2.5-flash / 2.0-flash / 2.5-flash-lite | 上限到達済み or 無料枠対象外 |
| **gemini-flash-lite-latest** | 使用可。**思考トークン0**で出力予算を食わない |

Gemini 3.x系は思考トークンが `maxOutputTokens` を消費する。「1+1は？」でも
47トークン使い、予算50では本文がゼロ件で返ってきた。`thinkingBudget` は
3.6-flash では400エラーになるため `thinkingLevel: LOW` を指定する。

---

## M3. プロンプト改善（過剰な拒否の解消）

- 日付: 2026-08-07
- モデル: gemini-flash-lite-latest
- 比較対象: M2b-2（同じモデル・同じ設問）

### 何を直したか

M2b-2 の失敗10件を「検索の失敗」と「回答の作り方の失敗」に切り分けたところ、
**正しいページを選んでおきながら「Wikiには記載がありません」と答える**ケースが
最悪だった（q30・q23）。36,261字の「駆動・フレーム班」を渡されて記載なしと答えていた。

原因は回答プロンプトの偏りだった。**「書かれていないことは推測で補うな」しか
書いておらず、「読み取れることは整理して答えてよい」を書いていなかった。**
小さいモデルほど字義通りに受け取る。

| 変更 | 内容 |
|---|---|
| 回答プロンプトの再調整 | 「答えられることは答える」節を追加。要約・比較・時系列の整理は推測ではないと明示 |
| **回答段にも目次を渡す** | 全体を見渡す問い（q31「最も情報が薄い分野は？」）は選択ページの本文だけでは構造的に答えられない。目次は先頭固定なのでキャッシュも効く |
| 選択プロンプトの補強 | 「質問の語がそのままタイトルに含まれるページは必ず候補に入れる」（q26で「合宿」ページを選べなかった） |

### 結果

| 指標 | M2b-2（改善前） | M3（改善後） |
|---|---|---|
| ページ選択の的中 | 24/29 (82.8%) | 24/29 (82.8%) |
| 出典の明示 | 31/31 (100%) | 31/31 (100%) |
| **回答の出し方が種別に対して適切** | 21/31 (67.7%) | **23/31 (74.2%)** |
| Faithfulness | 25/31 (80.6%) | 25/31 (80.6%) |

**狙った3件がすべて直った。**

| 問い | 改善前 | 改善後 |
|---|---|---|
| q30 歴代で駆動系の課題はどう変遷したか | 「記載がありません」 | 38代のテンショナー固定法の改善から始まる変遷を記述 |
| q23 構造設計は38代から41代で何が変化したか | 「記載がありません」 | 「41代ではチームトラブルにより構造設計が不在（蒸発）」 |
| q31 最も情報が薄い分野はどこか | 「記載がありません」 | 「パイロット分野。2ページしかなくどちらも中身なし」 |

q23 はゴールデンデータの期待回答とほぼ一致した。q31 は**目次を根拠に答えており**、
回答段に目次を渡した効果が直接出ている。

**Faithfulness は 80.6% で横ばい。** 「答えられることは答える」に寄せれば
裏付けの無い記述が増えるはずだが、実測では悪化しなかった。

### 判定器の欠陥を3つ潰した

Faithfulness が一度 74.2% まで下がったが、内訳を見ると**判定器側の誤判定**だった。

| 欠陥 | 症状 | 対処 |
|---|---|---|
| 判定器が目次を見ていない | 回答段に目次を渡したのに判定器には渡しておらず、目次を根拠にした記述（q31）を「資料にない」と指摘 | 判定器にも目次を渡す |
| 出典行を主張として扱う | 「出典: 構造設計 38th（最終更新: 2022-08-21）」そのものを裏付け無しと指摘 | 出典行を判定対象から除外 |
| 判定を先に書かせていた | 判定基準の文をオウム返しする（M2bで判明） | `unsupported_claim` を先に書かせる |

この3つを潰して 80.6%。**評価器そのものを検証する工程が要る**という
docs/01 §5-3 の設計判断が、そのまま実務として必要だった。

なお q20 の指摘は**判定器が正しかった**。回答が「最終更新2026年4月のため情報が
古い可能性があります」と書いていたが、当時まだ4ヶ月前であり、プロンプトの
「2年以上前なら」に反していた。判定器が回答の誤りを正しく捕捉した例。

### 残る課題

- **ページ選択は改善しなかった**（82.8%のまま）。q04・q24・q26 は検索段の失敗であり、
  回答段のプロンプトでは直らない。目次のリード文が空のページ（5件）が絡んでいる
- `global` は 1/3 のまま。q30 は直ったが他2件は未解決
- **判定器と人手評価の一致率（κ）は未測定。** Faithfulness 80.6% は、
  判定器の欠陥を3つ潰した後の値ではあるが、まだ人手で裏を取っていない

---

## M4. 目次のデータ品質改善（検索段）

- 日付: 2026-08-07
- モデル: gemini-flash-lite-latest（M3と同じ）
- 変更対象: **プロンプトではなく `build_index.py` の目次生成**

### 何を直したか

M3 で残った失敗（q04・q24・q26）は検索段の失敗であり、回答段のプロンプトでは
直らなかった。目次エントリを見ると原因が明確だった。

| ページ | 改善前のリード文 | 問題 |
|---|---|---|
| 代表資料 | `[41代鳥コン資料引継ぎ](https://docs.google.com/document/d/10iAY…` | **URLで90字を使い切り**、本文にある「鳥コン資料」が見えない |
| 40th | `全体スケジュール [翼班スケジュール](https://docs…` | 表の中の「2023年4月 コンセプト会議」が一切出ない |
| 合宿 | （空） | リード文そのものが無い（他4ページも同様） |

対処は3つ。

1. **リンクはラベルだけ残す**（`[ラベル](url)` → `ラベル`、裸のURLは削除）
2. **表しか無いページは表の中身も拾う**
3. **リード文が空なら最初の中身のある節から取る**

### 結果

| 指標 | M3 | M4 | 差 |
|---|---|---|---|
| **Page Recall@3（M2a）** | 86.2% | **93.1%** | **+6.9** |
| うち `fact` | 8/11 | **11/11** | **+3** |
| 目次サイズ | 14,743字 | 14,596字 | −147 |
| エンドツーエンドのページ的中 | 24/29 (82.8%) | 25/29 (86.2%) | +1 |
| Faithfulness | 25/31 (80.6%) | **26/31 (83.9%)** | +1 |
| 回答の出し方 | 23/31 (74.2%) | 23/31 (74.2%) | ±0 |

**狙ったq04・q24・q26がすべて直り、`fact` が満点になった。**

### 読み取れたこと

**データ品質の改善が、モデルを大きくするのと同じ効果を出した。**

M2a の Page Recall@3 は、ローカルの qwen3:30b-a3b が 93.1%、
gemini-flash-lite が 86.2% だった。**目次のリード文を直しただけで
flash-lite が 93.1% に並んだ。** パラメータ数で10倍以上違うモデルの差が、
入力データの整形で消えた。

docs/01 §6 で「データ品質がプロンプトより効く」と書いた見立てが、
モデルサイズとの比較でも裏付けられた形になる。**目次は毎回コンテキストに
載る唯一の入力**なので、ここの1文字あたりの価値が突出して高い。

**URLは目次では有害。** 目次の役割は「どのページを開くか判断させる」ことであり、
URLはその判断に一切寄与しない。にもかかわらず文字数を消費し、判断材料を押し出す。

### 残る課題

- **q17・q18 が未解決。** どちらも正解が `空力設計(41st)` だが、ハブページの
  `空力設計` を選んでしまう。根拠が19,448字のページの奥深く（他チーム分析表）に
  あるため、目次のリード文では表現しきれない
- `unanswerable` のページ的中が 0/2 になった（M3は1/2）。ただし忠実性は 2/2 で、
  「存在しないものを存在しないと言う」挙動自体は正しい
- 判定器と人手評価の一致率（κ）は依然として未測定

---

## 測定の型

新しい測定を足すときは、次の4つを必ず書いてください。

数字だけを貼っても、あとから読む人には何のために測ったのか分かりません。
逆に、**「何を判断したか」まで書いてあれば、同じ検討を繰り返さずに済みます。**

1. **日付 / 対象データの規模 / 再現コマンド**
   - 再現コマンドが無い数字は、あとで確かめようがありません
2. **数字**（表の形で）
3. **読み取れたこと** — その数字から何を判断したか
4. **判断を変えた点** — 設計方針のどこを書き換えたか

なお、**変えなかった場合も「変えなかった」と書いてください。**
測ったうえで維持したのか、単に測っていないのかが区別できなくなるためです。

## M5. 最新代の取りこぼし修正 / 個人情報マスクの見直し

- 日付: 2026-08-08
- モデル: gemini-flash-lite-latest

### 1. 最新代のページが引用されない問題

「空力設計の設計手順について詳しく説明して」で、40th と 38th は引用されるのに
**41st（最新代・19,454字と最大）が落ちる**事象を確認した。3回試して3回とも結果が違い、
41st が入るのは1回だけだった。

原因は3つ。

| # | 原因 |
|---|---|
| 1 | 空力設計は代違いで**4ページ**あるのに、選択上限が3ページ。構造的に全部入らない |
| 2 | 38th の節が「本を読む→概念設計→主翼設計→尾翼設計→TF」と**設計手順そのもの**。語彙一致では38thが圧勝する |
| 3 | 41st は19,454字と最大なのに節が4つだけで、**情報量に対して目次の記述が薄い** |

対処は2つ。

- 選択プロンプトに「**同じテーマで代違いのページが複数ある場合は最新の代を必ず含める**」を追加。
  引き継ぎ資料では最新代が最も重要であり、古い代だけを挙げるのは誤りである
- 選択上限を 3 → 4 ページに

結果、3回とも41stが入り、うち2回は1位になった。全体でも退行なし。

| 指標 | M4 | M5 |
|---|---|---|
| Page Recall@3 | 93.1% | 93.1% |
| **1位が正解** | 82.8% | **86.2%** |

### 2. 個人情報マスクが業務情報まで潰していた

「荷重試験の申請先メールアドレス」が答えられないという指摘から発覚。
メールアドレスを一律マスクしていたため、**申請先の窓口アドレスまで消えていた**。

実データ26件を分類したところ、業務用と個人用がきれいに分かれていた。

| | 例 | 件数 |
|---|---|---|
| 残す | `riko-reserve@list.waseda.jp` `classroom-reserve@list.waseda.jp` `reserve@sci.waseda.ac.jp` `info@wasa-birdman.com` | 12 |
| 伏せる | 早稲田の学生用サブドメイン（`@asagi.waseda.jp` 等）、役割語を含まない個人のフリーメール | 15 |

**電話番号のマスクは撤廃した。** 実データ3件がすべて業者（木村バルサ、トヨタレンタカー）で、
発注先・レンタル先の連絡先は業務情報だった。

あわせてメールの正規表現の欠陥を修正した。Python の `\w` は Unicode を含むため、
`wasa.birdman@gmail.comにWASAの通知が…` のドメイン部が日本語ごと取り込まれ、判定が狂っていた。

**教訓: 「個人情報だから伏せる」は粒度が粗すぎる。** 同じ形式のデータでも、
業務上の宛先と個人の連絡先は用途がまったく違う。実データを分類してから決めるべきだった。

---

## M6. 公式サイトの取り込み（出所を2つにする）

- 日付: 2026-08-08
- モデル: gemini-flash-lite-latest

### なぜ足したか

引き継ぎWikiは「作り方」に詳しい一方、**団体の成り立ち・歴代機体・対外的な説明が無い**。
「WASAとはどんなサークルか」「歴代の機体名と記録は」に答えられなかった。
一般公開の https://wasa-birdman.com/ を `dump_site.py` で取り込んだ。

### 数字

| 項目 | 実測 |
|---|---|
| 固定ページ | 31（本文80字未満の9件は除外） |
| 投稿（活動報告） | 479（2011〜2026年） |
| 索引に入る本文の総量 | 349,923字（取得時384,194字から短いページを除外・整形）。**Wiki本文（300,425字）より多い** |
| 索引全体 | 114 → 614ページ / 334 → 911チャンク |

WP REST API は無効（`/wp-json/` が404）。sitemap索引から辿った。
`<loc>` が CDATA で包まれており、素朴な `<loc>[^<]*</loc>` では1件も取れない。
ページは Elementor 製で `entry-content` が無く、`data-elementor-type="wp-page"` を本文とみなした。

### 目次の載せ方を3案で比べた

目次はプロンプト先頭に固定してキャッシュを効かせる設計なので、サイズがそのまま費用になる。

| 案 | 目次字数 | 倍率 | ¥/質問（キャッシュ読出） |
|---|---|---|---|
| v0 Wikiのみ | 15,030 | 1.00 | 0.45 |
| A 固定ページのみ | 19,442 | 1.29 | 0.58 |
| **B A+投稿を年別・タイトルのみ** | **25,475** | **1.70** | **0.80** |
| C A+投稿を1行・リード付き | 53,278 | 3.54 | 1.60 |

**Bを採った。** Cは費用が3.5倍になるうえ、ローカル測定用の32kコンテキストに収まらず
**測れなくなる**。かといって投稿を落とす（A）と、投稿が公式サイトの本文の94%を占めるため
取り込む意味がほぼ消える。タイトルが「第七回試験飛行報告」のように自己説明的なので、
選ぶだけならタイトルで足りる。

### 検索精度への影響：**決着していない**

目次あり／なしで3回ずつ測るつもりだったが、**Gemini無料枠の日次上限（500リクエスト）に
当たって完走しなかった**。取れたのは各1回のみ。

| 指標 | Wikiのみ 14,741字 | 公式サイト込み 26,718字 |
|---|---|---|
| Page Recall@3 | 93.1% | 89.7% |
| 1位が正解 | 86.2% | 82.8% |

差は **29問中1問（q26「合宿で39代はどこに行きましたか」）** であり、1問=3.4ポイント動く。
同じ設定で回して 93.1% と 89.7% の両方が出ており、**この差は実行ごとのばらつきと
区別がつかない**。公式サイト込みが劣ると結論づけるにはデータが足りない。

→ **翌日以降に各3回ずつ測り直す。** それまで「退行なし」とは書かない。

### 評価スクリプトの欠陥をまた1つ見つけた

`toc_eval.py` が**本番と違う照合をしていた**。本番の `resolve()` は目次由来の
`（別名: X）` を剥がしてから突き合わせるのに、評価は生の文字列で比べていた。
そのため本番では引けているページを「存在しないページ名」と数え、実力を過小に報告していた。

対処として `resolve_title()` を `rag/pipeline.py` のモジュール関数に切り出し、
**評価から必ず同じ実装を使う**ようにした。結果、架空ページ名は2件 → 0件。

**測定結果が動いたら、まず測定器を疑う**という原則が、また正しかった。

### データ品質で直した2点

| 症状 | 原因 | 対処 |
|---|---|---|
| `== 2019 / Canopus ==` と見出しが2行に割れる | `<h2>2019<br>Canopus</h2>` が実在。見出しの正規表現は1行前提 | 見出しの中身を1行に畳んでから出力 |
| 記事の冒頭に記事名が3回並ぶ | テーマがパンくず・見出し・本文頭で繰り返す | 連続する同一行を1回に。**定型行の除去より後**で消す（先だと先頭行が別の行で、499/510ページで素通りしていた） |

2点目は**処理の順序だけの間違い**で、コードは正しく見えていた。実データで
「何ページ残ったか」を数えて初めて気づいた。

### 副産物：APIキーがログに漏れる経路を塞いだ

429エラーの本文を読んでいて、**例外メッセージにAPIキーがそのまま入っている**ことに気づいた。

```
RuntimeError: https://...:generateContent?key=［APIキー］ が 429: {...}
```

Gemini はキーをクエリ文字列で受け取るため、Python の `RuntimeError` にも、
Go の `*url.Error`（`log.Printf("%v", err)` でサーバーログに出る）にも鍵が載る。
**測定ログやエラーログを誰かに見せた時点で漏れる。**

両方とも `x-goog-api-key` ヘッダ渡しに変更した。Python 側には保険として
URLのキーを伏字にする `redact()` も入れてある。

---

## M5. デプロイ前セキュリティ検査

### 再現コマンド

```bash
cd backend
go vet ./...
go test ./...
go run golang.org/x/vuln/cmd/govulncheck@latest ./...

cd ../web
npm audit --audit-level=moderate
npm run build

curl -X POST -H 'Origin: https://attacker.example' http://localhost:8080/api/logout
curl -X POST -H 'Origin: http://localhost:5173' http://localhost:8080/api/logout
```

### 結果

- Goの静的解析: 指摘0件
- Goのテスト: 全パッケージ成功
- Goの到達可能な既知脆弱性: 0件
- npmの既知脆弱性: 0件
- フロントエンドの型検査と本番ビルド: 成功
- Git管理対象と履歴の既知形式の秘密情報: 検出なし
- `.env`、`dump/`、`data/`、`eval/golden.json`、`eval/answers.json`: すべてGit除外を確認
- `ALLOW_ORIGIN=http://localhost:5173`時の許可外Origin: 403
- 同設定時の許可Origin: 200

### 読み取れたこと

- 現時点のコードと依存関係には、検査ツールで検出できる既知の問題はない
- CORSヘッダーだけではCookie付きPOSTのCSRF対策にならないため、許可外の`Origin`を403で拒否する必要がある
- Gemini無料枠は送信内容と生成結果がGoogleの製品改善に使われるため、氏名・役職などを残した非公開Wikiの本番送信先にはできない

### 判断を変えた点

- Cloud Runでは`SESSION_SECRET`と`ALLOW_ORIGIN`を必須にし、未設定なら起動を止める
- Cloud RunでGeminiを使う場合は、課金有効プロジェクトを確認した`GEMINI_PAID_TIER=true`を必須にする
- Dockerのフロントエンド依存関係は`npm ci`だけで固定し、lockfile不一致時の`npm install`へのフォールバックを廃止する

---

## M7. 最終更新日と内容の年代の食い違い / 事実カードの追加

利用中に見つかった2つの誤答から始めた。どちらもデータの読み方の問題であって、
検索精度の問題ではない。

### きっかけになった誤答

1. 「鳥人間コンテストまでにやるべきことを教えて」に対し、
   **「情報が2年前（2026年4月）のものであるため」**と述べた。2026年4月は
   当時から4か月前であって2年前ではない。**プロンプトに今日の日付が無く、
   モデルが「何年前か」を勝手に見積もっていた。**
2. WASA鳥人間プロジェクトの年数を問われ、**公式サイトの「WASA60周年」につられて
   1985年始まりで60年目**と答えた。60周年はWASA全体（1965年設立）の話であり、
   鳥人間プロジェクト（1985年発足）の年数ではない。

### 数字

`build_index.py` にチャンク単位の年代抽出（`extract_era`）を足して測った。
本文中の西暦（`2023年` `2023/8/25` のように区切りが続くものだけ）と代を拾い、
代は `N代 = 鳥人間コンテスト(1984+N)年大会` で西暦に直して合流させている。

| 項目 | 実測 |
|---|---|
| 全チャンク | 911 |
| 本文から年代を拾えた | 565（62%） |
| 拾えなかった | 346（38%） |
| **本文が扱う年代が最終更新より2年以上古い** | **155（17%） / 89ページ** |

食い違いの内訳（本文側の最新年）: 2022年 38件 / 2023年 25件 / 2024年 16件 ほか。

代表例が、まさに誤答1の対象だった「鳥コン」ページである。

```
（出所: Wiki / ページ: 鳥コン / 最終更新: 2026-04-12 / 本文の年代: 2024年）
```

公式サイトの「WASA60周年記念交流会のお知らせ」も同じ形で、
最終更新は2026-03だが本文の日付は**2025年11月**である。

### 読み取れたこと

- **`last_edited` を情報の鮮度として使うのは誤り。** 6件に1件で2年以上ずれる。
  誤字直しやリンク追加でも更新日は今日になるので、原理的にずれる指標だった
- **代（世代）は西暦に決定的に変換できる。** `N代 = (1984+N)年大会` は、
  公式サイトの歴史ページの全代（1985年=初代 / 2005年=21代 / 2015年=31代）と、
  Wiki側の「2024年度執行代（40代）」「2026年TF(42nd)」のすべてに例外なく一致する。
  LLMに推測させる必要はない
- **既存の `GEN_BODY` は日本語で最も普通の書き方に当たらない。**
  `\b(3[0-9]|4[0-9])\s*(?:st|nd|rd|th|代)\b` は、Pythonの `\b` が日本語の文字も
  語構成文字とみなすため「42代の引き継ぎ」「第42代」のどちらにもマッチしない。
  ページ単位の `gen` はこの挙動のまま M2a の数字を出しているので触っていないが、
  **`gen` の抽出率（不明484件）はこれが理由で低く出ている可能性が高い**。要再測定
- **60周年の誤りは検索でもプロンプトでも直らない。** 資料には
  「WASAは1965年設立」も「WASA60周年」も「鳥人間は1985年」も正しく書いてあり、
  どれを引いても矛盾しない。**足りないのは2つが別の単位だという知識**である

### 判断を変えた点

- **今日の日付をプロンプトに渡す。** 日本時間で `answerPrompt` の先頭に入れる。
  Cloud RunのコンテナはUTCなので、JSTへの変換を明示する
- **チャンクに「本文の年代」を持たせ、資料ブロックに添える。**
  回答プロンプト側では「古さの判断は最終更新日ではなく本文の年代を根拠にする」
  「本文の年代が無いときは古さを断定しない」と指示する。空欄を出すと
  「不明」が「古い」に読み替えられるため、拾えなかったときは項目ごと省く
- **人が保守する事実カード `facts.md` を新設する。** `build_toc.py` が
  `data/toc.md` の先頭に埋め込む。目次は「先頭に固定するとキャッシュが効く」
  条件で設計されているため、目次の先頭に入れればキャッシュの条件も配置も変わらない。
  現在975字（目次全体の3.7%）。**「実際に誤答した」根拠のある事柄だけを書く**
- **資料ブロックにページURLを添え、出典をMarkdownリンクで出させる。**
  画面側（`web/src/markdown.tsx`）は以前からリンク記法と裸のURLの両方を
  リンクにしていたが、**モデルにURLを渡していなかったので出典が素のテキストだった**

### 残る課題

- 年代を拾えないチャンクが38%ある。手順書のように年代を書く必要がない文章も
  多いので、これ自体は異常ではない。ただし「古さを断定しない」に倒しているため、
  古い情報が古いと分からないまま出る余地は残る
- `GEN_BODY` の `\b` 問題の修正は、`gen` と `team` の分類、ひいては目次の並び順を
  動かす。M2a を測り直せる状態を作ってから直すこと
- **Gemini APIのクレジットが切れており、この変更のエンドツーエンドの回答品質は
  測れていない。** プロンプトの組み立て（日付・年代・URL・事実カードの位置）は
  スタブLLMで確認済み。回答が実際に改善したかは未検証

---

## M8. TR797の取りこぼし / 型番の正規化一致

- 日付: 2026-08-09
- きっかけ: 実際の利用時に「TR797とは何ですか？」へ
  **「資料内に記載なし」**と誤答した。一方、後続の循環分布の質問では
  同じ資料にある「TR-797型分布」を使って回答した
- 再現コマンド: `python eval/retrieval_eval.py`
- 対象: チャンク911件、ゴールデンデータ32問（検索採点対象30問）

### 数字

誤答を再現するため、q32「TR797とは何ですか？」をゴールデンデータに追加した。
正解は「空力設計(38th) > 主翼設計」の `p14-c1` とし、実データに照合した。

| 項目 | 結果 |
|---|---:|
| q32の正解チャンク順位（文字bigram BM25） | **22位** |
| q32 Evidence Recall@5 | **0%（上位5件に入らない）** |
| 全30問 Evidence Recall@5 | 16/30（53.3%） |
| 全30問 Page Recall@5 | 20/30（66.7%） |

修正後の決定的な経路は、次のコマンドで検証した。

```bash
cd backend
GOCACHE=/private/tmp/wasa-go-cache go test ./internal/pipeline \
  -run Identifier -v
```

結果: **2テストともPASS**。LLMが誤って人物ページと型番のない節だけを選んでも、
質問の `TR797` と本文の `TR-797` を同一視し、「空力設計」の該当節を回答文脈へ入れる。

### 読み取れたこと

- **「資料にあるのに記載なし」は回答プロンプトでは直らない。** TR797は目次の
  ページ名・リード文・上位見出しに現れず、ページを開く前の段階で落ちていた
- **BM25を入れるだけでも型番が必ず上位になるとは限らない。** 今回は完全一致語を
  含む正解チャンクが22位で、上位5件の回答文脈には入らなかった
- `TR797` / `TR-797` のような記号差は意味推論ではなく、決定的な正規化で扱える

### 判断を変えた点

- 目次方式だけで進める方針は維持するが、**英字で始まり数字を含む4文字以上の型番**が
  質問にある場合だけ、全チャンクを正規化完全一致で総当たりする軽量経路を追加した
- ハイフンとアンダースコアを除いて小文字化し、一致ページ上位2件をLLMの選択結果へ
  合流する。4枠すべてを完全一致で埋めず、質問全体を見たLLM候補を2枠以上残す。
  長いページから節を8件へ絞る段でも、型番一致チャンクを先に残す
- TR797を `facts.md` へは追加しない。資料本文に説明があるため、事実カードで
  検索漏れを隠さず、検索経路とゴールデンデータで再発を防ぐ

### 残る課題

- Gemini APIを使ったエンドツーエンド回答は再測定していない。今回保証したのは、
  型番を含む正解ページが回答文脈へ入るところまでである
- 日本語の略語や、数字を含まない `TF`・`FFC` などは今回の対象外。用語集または
  BM25が必要かは、それらの実際の誤答をゴールデンデータへ足してから判断する

---

## M9. 追質問の検索 / 汎用とアシスタントの知識境界

- 日付: 2026-08-09
- きっかけ: TR797の質問では「記載なし」と答えた後、循環分布の質問では
  TR-797を使って回答した。画面には会話履歴があったが、質問APIは現在の質問しか
  パイプラインへ渡しておらず、検索上は毎回独立した一問一答だった
- 再現コマンド: `python eval/retrieval_eval.py`
- 対象: チャンク911件、ゴールデンデータ33問（検索採点対象31問）

### 数字

q33として、直前に「完全楕円循環分布とTR-797型分布」と答えた後、
「後者について詳しく教えてください」と追質問する問題を追加した。正解チャンクは
「空力設計(41st) > 循環分布の決定」の `p146-c6` で、実データに照合した。

| 検索へ渡す内容 | q33の正解チャンク順位 |
|---|---:|
| 現在の質問「後者について詳しく」だけ | **342位** |
| 直近の質問・回答を添える | **1位** |

履歴込みで33問を測った全体値は次の通り。

| 指標 | 結果 |
|---|---:|
| Evidence Recall@5 | 17/31（54.8%） |
| Page Recall@5 | 21/31（67.7%） |
| MRR | 0.366 |

### 読み取れたこと

- **履歴を保存・表示するだけではチャットにならない。** 「後者」のような指示語は、
  現在の質問だけでは検索不能に近く、直近会話を添えることで初めて正解へ到達した
- 全履歴は不要。今回の失敗は直前1往復で解ける。入力費用を会話長に比例させず、
  直近2往復を上限とするのが妥当
- 過去の回答をそのまま根拠にすると、以前の誤答を次へ引き継ぐ。履歴は参照解決だけに使い、
  事実は今回選んだ資料で再確認する必要がある

### 判断を変えた点

- `/api/ask` に直近2往復を追加し、ページ選択・型番検索・節選択・回答生成へ渡す。
  サーバーでも質問500字、回答2,000字、2往復を上限として検証する
- 汎用チャットは、WASA資料で分かる内容を先に答えたうえで、一般的な概念説明を
  **「一般知識（WASA資料外）」へ分離した場合だけ**補足できるようにする
- アシスタントは従来どおり資料範囲内に閉じる。汎用との違いは別モデルではなく、
  参照範囲と回答形式、および資料外知識を許すかどうかである

### 残る課題

- q33で測れたのは検索段だけ。実際のモデルが「後者」を正しく説明するかは、
  APIクレジット復旧後にエンドツーエンドで再測定する
- **汎用と各アシスタントの回答精度差は未測定。** 同じモデルを使うため、現時点で
  「アシスタントの方が何ポイント高い」とは言えない。空力・駆動から同一問題を
  3回ずつ比較し、Evidence Recall、誤った回答不能、Faithfulness、必要事項の網羅を測る
- 資料外一般知識の正しさはWikiへのFaithfulnessでは測れない。WASA固有の主張と
  正しく分離できたかをルールで見たうえで、一般部分は少量の人手評価が必要

比較用に `python eval/assistant_retrieval_eval.py` を追加し、空力2問・駆動1問を
汎用と該当区分アシスタントで各3回（予定18リクエスト）実行できるようにした。
2026-08-09の実行は**1件目からHTTP 429（プリペイド残高枯渇）となり、完了0件**だった。
このため精度差の数値は出していない。なお、回復しない残高枯渇に対して5・10・20・40秒の
計75秒を待っていたため、評価用LLMクライアントはこのエラーだけ即時停止するよう変更した。

---

## M10. プロンプトから必要性を説明できない文を除く

- 日付: 2026-08-09
- きっかけ: モデルはリクエストごとに必要な文脈を受け取る一方、部署名のような
  正答に影響しない設定や、同じ規則の長い説明は入力費用と読み落としを増やすという指摘
- 再現コマンド:

```bash
git show HEAD:backend/internal/pipeline/pipeline.go | ruby -rjson -e \
  's=$stdin.read; [["selectPrompt","var selectSchema"],["answerPrompt","// scopedTOC"]].each do |name, stop|; a=s.index("const #{name} = "); b=s.index(stop,a); expr=s[a...b]; value=expr.scan(/`([^`]*)`|"((?:\\.|[^"])*)"/m).map{|raw, quoted| raw || JSON.parse(%Q{"#{quoted}"})}.join; puts "#{name}: #{value.length}字 / #{value.lines.length}行"; end'

python -c 'from pathlib import Path
s=Path("backend/internal/pipeline/pipeline.go").read_text()
for name in ("selectPrompt","answerPrompt"):
    marker=f"const {name} = `"; start=s.index(marker)+len(marker); end=s.index("`\n",start)
    value=s[start:end]; print(f"{name}: {len(value)}字 / {len(value.splitlines())}行")'
```

### 数字

| プロンプト | 変更前 | 変更後 | 削減率 |
|---|---:|---:|---:|
| ページ選択 | 571字 / 20行 | 310字 / 19行 | **45.7%** |
| 回答生成 | 1,395字 / 51行 | 611字 / 34行 | **56.2%** |
| 合計 | 1,966字 | 921字 | **53.2%** |

### 読み取れたこと

- 長かった理由は役割を具体的にしたためではなく、年代誤判定の実測値や例、出所の説明を
  規則の中でも繰り返していたためだった。モデルに必要なのは判断基準であり、その根拠となる
  17%などの測定値はこの文書へ置けばよい
- 削除できなかった規則には、架空ページ名、最新代の落選、無根拠な「記載なし」、誤った前提、
  最終更新日の誤用、出典URLの創作という実際の失敗がそれぞれ対応している

### 判断を変えた点

- 役割はsystemに一度だけ置き、各userプロンプトは「タスク」「規則」「入力」の順にする
- プロジェクト内の呼び方を初出で説明する。`引き継ぎWiki` は部内資料、`公式サイト` は
  一般公開資料、`本文の年代` は本文中の西暦・代から抽出した値として資料データ側で示す
- 今後は規則追加の前に、どの誤答分類を防ぐ文かを評価結果と対応付ける。防ぐ失敗を説明できない
  文は足さず、指摘漏れと過剰検知を個別に見てプロンプトまたはデータセットへ戻す

### 残る課題

- 文字数を減らしたこと自体は精度向上の証拠ではない。33問のページ選択と回答品質を再測定し、
  既存の失敗分類ごとに退行がないか確認する必要がある

---

## M11. 実機写真を基準にしたロゴ画像の検査

- 日付: 2026-08-09
- 入力: ローカルの `web/public/assets/IMG_4330.JPG`（人物を含むためコミット対象外）
- 再現コマンド:

```bash
cd web
sips -g pixelWidth -g pixelHeight -g hasAlpha \
  public/assets/wasa-chat-mark-photo-trimmed.png \
  public/assets/wasa-chat-logo-photo-trimmed.png \
  public/assets/wasa-aircraft-wireframe.png
npm run build
```

### 数字

| アセット | 寸法 | 透過 |
|---|---:|---|
| 単体マーク | 1,177 × 902 px | あり |
| 横長ロゴ | 1,697 × 494 px | あり |
| 機体ワイヤーフレーム | 1,672 × 941 px | なし |

### 読み取れたこと

- 実機写真から先にワイヤーフレームを作ることで、長大な主翼、上反した翼端、吊り下げ式の
  ポッド、後方ブームと尾翼を、汎用的な飛行機記号へ置き換えずロゴへ残せた
- 初回線画には写真の翼紐が孤立した斜線として残った。修正版では翼紐だけを除き、
  ポッドを吊るす2本の支持部材は維持した。単体マークと横長ロゴも修正版から作り直した

### 判断を変えた点

- 生成画像へ直接「飛行機のロゴ」を依頼せず、`実機写真 → 技術線画 → 単純化したマーク →
  横長ロゴ` の順で形状を固定する
- 画面では横長ロゴをログイン・サイドバー・初期画面に使い、回答者アイコンだけ単体マークを使う

---

## M12. 回答モード比較の接続確認

- 日付: 2026-08-09
- 再現コマンド:
  `LLM_PROVIDER=gemini python eval/response_mode_eval.py --questions 1 --repeats 1`
- 計画: 層化した1問について、fast / standard / deepのページ選択を各1回（計3リクエスト）

### 数字

| 項目 | 結果 |
|---|---:|
| 完了したリクエスト | **0/3** |
| 失敗した位置 | **1件目** |
| HTTP状態 | **429** |

エラーはM9と同じ`prepayment credits are depleted`であり、モデルやthinking設定の
優劣を示す結果ではない。回復しない残高不足では再試行せず、その場で停止した。

### 読み取れたこと

- モード比較は現状のAPI残高では測定不能
- 1問だけの接続確認で止めたため、未測定設定による費用消費は広げていない
- 画面とAPIはモードを受け付けるが、`GEMINI_FAST_MODEL`、`GEMINI_STANDARD_MODEL`、
  `GEMINI_DEEP_MODEL`が空なら同じ既定モデルへ戻る

### 判断を変えた点

- 強いモデルを標準・じっくりへ既定で割り当てるのは見送る
- 残高復旧後、10問×3モード×3反復（計90ページ選択）を先に測る
- Page Recallと待ち時間の改善を確認した組み合わせだけ、本番環境変数へ設定する

---

## M13. 引き継ぎ文書と実装の整合性監査

- 日付: 2026-08-09
- 対象: 現行`data/index.json`、Goバックエンド、React画面、README、AGENTS、仕様書
- 再現コマンド:

```bash
python3 -c 'import json,collections,statistics,pathlib; ps=json.load(open("data/index.json"))["pages"]; cs=[c for p in ps for c in p["chunks"]]; print(len(ps), len(cs), sum(p["chars"] for p in ps), pathlib.Path("data/index.json").stat().st_size); print(collections.Counter(p["source"] for p in ps)); print(statistics.median(c["chars"] for c in cs), min(c["chars"] for c in cs), max(c["chars"] for c in cs))'
rg -n '03-画面・認証仕様|まだ動くボット|gemini-flash-latest|同じ段構成・同じプロンプト' README.md AGENTS.md docs backend rag web/src
python3 -c 'import re,pathlib; fs=[pathlib.Path("README.md"),pathlib.Path("AGENTS.md"),*pathlib.Path("docs").glob("*.md")]; links=[(f,t.split("#",1)[0]) for f in fs for t in re.findall(r"\[[^\]]*\]\(([^)]+)\)",f.read_text()) if not t.startswith(("http://","https://","#"))]; bad=[(f,t) for f,t in links if t and not (f.parent/t).exists()]; print(len(links), len(bad))'
```

### 数字

| 項目 | 現行値 |
|---|---:|
| 索引ページ | **614**（Wiki 114 / 公式サイト500） |
| 検索対象ページ | **604** |
| チャンク | **911**（Wiki 349 / 公式サイト562） |
| 索引本文 | **646,856字** |
| `index.json` | **3,068,765 bytes** |
| チャンク文字数 | 中央値626 / 最小22 / 最大2,450 |
| 文書内のローカルリンク | **40件 / リンク切れ0件** |

### 見つかった乖離

- `AGENTS.md`が「まだ動くボットはない」としていたが、Go / Reactの初期Webアプリは実装済みだった
- `AGENTS.md`とREADMEの対象規模が、公式サイト追加後の614ページ・911チャンクを反映していなかった
- READMEとコードコメントがPython / Goを「同じプロンプト」と断定していたが、Goだけに
  system規則、決定的な参照範囲、SSE、認証がある
- READMEとGeminiクライアント直接利用時の既定値が`gemini-flash-latest`のままで、
  本番起動経路の固定ID`gemini-3.5-flash-lite`と違っていた
- 旧「画面・認証仕様」に、公式サイト限定時の目次を「差し替える」と「差し替えない」が同居していた
- アシスタントを変えても同じチャットへ追記でき、参照範囲・一般知識の可否・口調が履歴内で混ざり得た

### 読み取れたこと

- 一つの長い仕様書へ画面、認証、プロンプト、決定的ルールを混ぜると、同じ機能の説明が
  離れた位置で矛盾しても見つけにくい
- Pythonはコア検索・回答の測定経路であり、本番の共有アシスタントや認証を再現するものではない
- アシスタント切り替えは表示設定の変更ではなく、回答の知識境界を変える操作として扱う必要がある

### 判断を変えた点

- 引き継ぎ文書を入口、画面、デプロイ、system、決定的ルール、認証へ分割した
- 実行されるプロンプトと規則の最終的な正本はコードと明記し、文書は責務と検証方法を示す
- アシスタントを実際に変更した場合だけ、新しいチャットを開く
- 同じアシスタントの再選択では不要な空履歴を増やさない
- Geminiの未指定時既定モデルを固定IDへ統一し、再現不能な`latest`別名を使わない

---

## M14. 利用者フィードバック導線の実装前基準

- 日付: 2026-08-09
- きっかけ: 自動評価だけでは、実利用者が何を役立つ・使いにくいと感じたか分からなかった
- 再現コマンド:

```bash
git grep -n 'feedback\|フィードバック' 132047f -- web/src backend/internal/server backend/internal/state
GOCACHE=/private/tmp/wasa-go-cache go test ./internal/server -run Feedback -v
cd web && npm run build
```

### 数字

| 項目 | 実装前 | 実装後 |
|---|---:|---:|
| フィードバックAPI | **0** | **2**（送信 / 管理者一覧） |
| 回答評価 | **0** | **2**（👍 / 👎） |
| 任意の回答理由 | **0** | **9**（良い3 / 改善6） |
| 画面全体の報告分類 | **0** | **5** |
| フィードバック専用サーバーテスト | **0** | **5** |
| 本番JavaScript | — | **507.12kB**（gzip 154.99kB） |

### 読み取れたこと

- 実装前はゴールデンデータ33問の自動・LLM評価だけで、実利用者の評価を受け取る経路が無かった
- 文章を必須にすると送信負担が高いため、分類または👍／👎を押した時点で保存する必要がある
- 1タップ評価だけでは原因が分からないため、理由チップと500文字の補足を任意で足せる形が必要
- 報告を集めても保守者が見られなければ改善へ繋がらないため、管理者一覧も同時に必要
- 本番JavaScriptはViteの500kB警告を超えたため、機能追加とは別に遅延読込を検討する余地がある

### 判断を変えた点

- 右上の重複した「WASA」文字を削除し、「改善を送る」へ置き換える
- 同じ回答への追加理由・補足は新規レコードではなく上書きし、件数を水増ししない
- 回答評価には質問・回答・出典も保存することを押す前に表示する
- 報告者のWiki利用者名は保存せず、HMACキーも管理者APIへ出さない
- 実利用データはまだ0件なので、👍率や👎率の合格基準は推測で決めない

---

## M15. Markdown・KaTeXの遅延読込

- 日付: 2026-08-09
- きっかけ: M14で本番JavaScriptがViteの500kB警告を超えた
- 再現コマンド:

```bash
cd web
npm run build
```

### 数字

| 項目 | 分割前 | 分割後 | 変化 |
|---|---:|---:|---:|
| 初回JavaScript | **507.12kB** | **243.23kB** | **52.0%減** |
| 初回JavaScript（gzip） | **154.99kB** | **76.20kB** | **50.8%減** |
| 初回CSS | **63.04kB** | **32.85kB** | **47.9%減** |
| 遅延JavaScript | **0** | **263.76kB**（gzip 78.67kB） | 回答時に読込 |
| 遅延CSS | **0** | **30.19kB**（gzip 8.08kB） | 回答時に読込 |
| 500kB警告 | **1件** | **0件** | 解消 |

### 読み取れたこと

- 初回バンドルの約半分は、回答が表示されるまで不要なMarkdown・KaTeXだった
- Reactの画面本体だけを初回チャンクへ残すことで、機能と総転送量を変えず初期読込を半減できた
- 遅延チャンクを回答が届いてから読み始めると表示待ちが起こり得るため、質問を送った時点で先読みする
- KaTeXのCSSとフォント参照も遅延チャンク側へ分かれ、ログイン画面では読み込まれない

### 判断を変えた点

- Markdown・KaTeXは静的importをやめ、`React.lazy`と`Suspense`で回答表示時だけ読み込む
- 分割数を増やすこと自体は目的にせず、500kB警告を解消した現段階では追加分割を推測で行わない
- アシスタント画面や管理者一覧の追加分割は、今後の機能追加で初回チャンクが再び大きくなった場合に測定する

---

## M16. TR797と追質問の対象限定E2E

- 日付: 2026-08-09
- きっかけ: q32・q33の検索段は直っていたが、実モデルの回答品質とHTTP層を含む接続が未検証だった
- 再現コマンド:

```bash
LLM_PROVIDER=gemini \
GEMINI_MODEL=gemini-3.5-flash-lite \
GEMINI_FAST_MODEL=gemini-3.5-flash-lite \
GEMINI_STANDARD_MODEL=gemini-3.5-flash-lite \
GEMINI_DEEP_MODEL=gemini-3.5-flash-lite \
python eval/answer_eval.py --ids q32,q33 --output eval/answers-tr797.json

cd backend
GOCACHE=/private/tmp/wasa-go-cache go test ./internal/server \
  -run 'TestAskCarriesTR797|TestValidConversation|TestResponseMode' -v
```

### 数字

| 項目 | 結果 |
|---|---:|
| 対象限定評価 | **2/33問**（q32・q33） |
| Geminiで完了した設問 | **0/2** |
| 失敗した位置 | **q32のページ選択（1件目）** |
| HTTP状態 | **429**（`prepayment credits are depleted`） |
| 決定的HTTP E2E | **2/2 PASS**（単独質問 / 追質問） |
| 会話入力検証 | **7/7条件 PASS** |
| 回答モード検証 | **6/6値 PASS**（有効5 / 無効1） |

### 読み取れたこと

- モデル一覧42件の取得には成功したため、APIキーやネットワークの不備ではなく生成用残高の問題である
- 残高不足は待っても回復しない429として判別され、再試行せず1件目で停止した
- 実モデルがTR797を正しく説明するかは、今回も未測定である
- 一方、決定的E2Eではブラウザ相当のJSONを質問APIへ送り、Cookie認証、入力検証、型番検索、
  会話中の「後者」の解決、節絞り込み、回答プロンプト、出典SSE、完了SSEまで2ケースとも通った
- LLMが意図的に人物ページと誤った節を選ぶスタブでも、TR-797の正解資料が回答プロンプトへ残った

### 判断を変えた点

- `answer_eval.py`へ`--ids`と`--output`を追加し、実際に誤答した設問だけを低コストで再測定できるようにする
- HTTPから回答生成までの決定的な接続はCIで固定し、モデル品質だけを残高復旧後の測定対象として分離する
- 生成0件を精度改善の証拠にはせず、q32・q33の実モデル結果は未測定表示を維持する

---

## M17. 回答モード・フィードバック操作・段階別所要時間

- 日付: 2026-08-09
- きっかけ: 回答モード4択は利用者に細かすぎ、画面全体の報告は分類タップだけで即送信されて誤操作し得た
- 再現コマンド:

```bash
git grep -n 'value: "fast"\|value: "standard"\|sendGeneralFeedback(reason)' 3bef08f -- web/src/App.tsx

cd backend
GOCACHE=/private/tmp/wasa-go-cache go test ./internal/pipeline ./internal/server ./internal/state

cd ../web
npm run build
```

### 数字

| 項目 | 変更前 | 変更後 |
|---|---:|---:|
| 画面で選べる回答モード | **4** | **2**（自動 / じっくり） |
| 分類選択時の送信 | **1リクエスト** | **0リクエスト** |
| 画面報告の明示送信ボタン | **0** | **1** |
| スマホの分類・送信タップ領域 | 内容依存 | **44px以上** |
| 保存する所要時間の段階 | **0** | **4**（ページ / 節 / 回答 / 合計） |
| 段階別時間の専用パイプラインテスト | **0** | **1** |
| 同時に開くヘッダーメニュー | 最大 **2** | 最大 **1** |
| デスクトップのアシスタント一覧ヘッダー | **2段** | **1段** |
| 汎用アシスタント画像 | ロボット | **WASA Chatマーク** |
| 履歴スクロールバー | ブラウザー既定 | **#3a3734** |
| 初回JavaScript | 243.23kB（gzip 76.20kB） | **243.64kB**（gzip 76.37kB） |
| 初回CSS | 32.85kB（gzip 6.85kB） | **35.45kB**（gzip 7.39kB） |

### 読み取れたこと

- 利用者が求めていない速度の細分を見せると、質問前に不要な判断を増やす
- 「自動」は内部でfast / standard / deepを使い分けられるため、明示指定は「じっくり」だけ残せばよい
- 分類ボタンと送信を同じ操作にすると、内容確認や補足入力の前に保存され、誤操作を取り消せない
- 通知・プロフィール・改善報告は、開いたメニュー以外を閉じれば重なりを防げる
- 履歴の暗色スクロールバー、WASA Chatマーク、1段の一覧操作により、サイドバーとアシスタント一覧の視認性を保ったまま密度を上げられる
- 旋回軌道、機体のバンク、軌跡を別々に動かすと、小さい回答待ち表示でも飛行機の旋回と読み取れる
- ページ選択、節選択、回答生成の時間をSSEで流し、回答履歴と回答評価へ保存すれば、
  `回答が遅い`という主観を同じ回答の実測値と照合できる
- 実利用の所要時間はまだ0件なので、遅いと判定する秒数や段階別の目標値は決められない

### 判断を変えた点

- 通常画面は「自動・じっくり」の2択とし、旧端末のfast / standard保存値はautoへ移行する
- APIは旧画面と自動判定のため4段階を維持し、生のモデルIDを引き続き拒否する
- 画面全体の報告は分類選択、任意補足、明示送信の順にする
- スマホではフィードバックを画面幅へ固定し、分類を1列で並べ、縦方向だけスクロールさせる
- 所要時間は30分以下かつ各段階が合計以下であることをサーバーでも検証する

---

## M18. 検索失敗の分類と実在タイトル候補

- 日付: 2026-08-09
- きっかけ: Evidence Recall@5が17/31のままだが、失敗原因を見ずにBM25やベクトル検索を追加すると、どの欠落に効いたか分からない
- 再現コマンド:

```bash
python eval/retrieval_eval.py

PYTHONPYCACHEPREFIX=/private/tmp/wasa-pycache \
  python3 -m py_compile rag/pipeline.py eval/retrieval_eval.py

cd backend
GOCACHE=/private/tmp/wasa-go-cache go test ./internal/pipeline
```

### 数字

| 項目 | 変更前 | 変更後 |
|---|---:|---:|
| BM25 Evidence Recall@5 | 17/31（54.8%） | **17/31（54.8%）** |
| BM25 Page Recall@5 | 21/31（67.7%） | **21/31（67.7%）** |
| BM25 MRR | 0.366 | **0.364** |
| LLMなしで正解ページを保証 | 2/31（6.5%） | **20/31（64.5%）** |
| 1問あたりの決定的候補 | 平均0.13件 | **平均1.16件** |
| 決定的候補の上限 | 2件 | **2件** |
| 専用Goテスト | 0件 | **3件** |

BM25の上位5件から外れた14問を実際の候補と正解チャンクで照合した。

- 質問に実在ページ名または世代別ページ名がある: **9問**
  - q08 / q10 / q11 / q19 / q21 / q22 / q25 / q26 / q29
- 型番の本文一致で保持できる: **1問**
  - q32
- タイトル完全一致では解けない: **4問**
  - q07（作業場→作業場情報）
  - q18（質問の滑空機と資料の人力プロペラ機という前提の違い）
  - q20（鳥人間コンテスト→鳥コという言い換え）
  - q30（歴代の複数ページを統合する全域型）

TR797のq32はゴールデンデータも再照合した。`p14-c1`も利用法の根拠になるが、
`p146-c6`が「翼根曲げモーメントをβ倍に制約したときの誘導抗力最小分布」と直接定義しているため、
正解ページを「空力設計(41st)」、正解チャンクを`p146-c6`へ差し替えた。

### 読み取れたこと

- 失敗14問のうち10問は、意味検索の不足より、質問中の実在タイトルをモデルのみに任せていたことが原因だった
- `40th`と`40代`を同じ世代とし、分野名と世代の両方が合うページを優先すれば、語順が違っても世代別資料を拾える
- 20/31は最終Page Recallではなく、**LLMが誤選択しても正解ページを落とさない下限**である
- 決定的候補を最大2件にすれば、合計4件の残り2枠以上で従来のLLM選択を保持できる
- BM25のEvidence Recall自体は改善していない。この数字をページ候補の64.5%と混同しない

### 判断を変えた点

- 質問中の実在ページ名と型番一致を、合わせて最大2件までLLM候補より先に合流する
- `40th / 40代`の表記差と記号の有無を正規化する
- `PM`が`RPM`に部分一致しないよう、3文字以下の英数字タイトルは単語完全一致だけに限る
- ページ候補でもアシスタントの出所・区分範囲外は決定的に除外する
- 残る4問のためにBM25やクエリ書き換えをすぐ追加せず、まず残高復旧後の実モデルページ選択で不足が残るか測る

---

## M19. フィードバック通知・会話参照・更新作業の回帰検査

- 日付: 2026-08-09
- きっかけ: フィードバックが管理者へ届かず、直前の「尾翼設計」を「それ」で追質問すると
  話題を見失った。また、索引にはあるメインページの過去問リンクを「資料にない」と誤答した
- 再現コマンド:

```bash
cd backend
GOCACHE=/private/tmp/wasa-go-cache go test ./...

cd ../web
npm run build

cd ..
python3 -m unittest test_rebuild.py
python3 -m py_compile rebuild.py check_updates.py dump_wiki.py dump_site.py build_index.py build_toc.py
python3 rebuild.py --dry-run

wc -l web/src/App.tsx web/src/feedback.tsx backend/internal/server/server.go
```

### 数字

| 項目 | 変更前 | 変更後 |
|---|---:|---:|
| メール通知経路 | **0** | **1**（SMTP / STARTTLS） |
| メール障害時に残る保存経路 | **0** | **1**（Firestore） |
| フィードバック通知テスト | **0** | **2**（成功 / 通知失敗） |
| 今回追加した検索・会話プロンプト回帰テスト | **0** | **3** |
| 資料更新に必要な手入力コマンド | **5** | **1**（`python rebuild.py`） |
| 公開元の変更確認用コマンド | **0** | **1**（`python check_updates.py`） |
| `App.tsx` | **1,854行** | **1,685行** |
| 抽出したフィードバック共通部品 | **0行** | **172行** |
| `server.go` | **1,026行** | **1,013行** |
| 初回JavaScript | 243.64kB（gzip 76.37kB） | **243.78kB**（gzip 76.56kB） |
| Go全パッケージ | — | **10/10 PASS** |
| 実SMTPへの送信 | — | **0件（本番設定前）** |

### 読み取れたこと

- 従来の送信先はFirestoreだけで、管理者メールへ届く経路が存在しなかった
- Firestoreへ先に保存すれば、SMTPの一時障害を画面へ伝えつつ報告内容を失わない
- 「それ」のための直近2往復は既にAPIへ渡っていたが、対象が一つに定まるときの回答規則が弱く、
  モデルが話題をWASA全体へ戻せた
- 過去問リンクは`data/index.json`のメインページ後半に存在していた。目次の短いリード文へは
  出ていないため、URLを求める質問だけ本文中のリンクを決定的候補として照合する必要があった
- 回答内の出典一覧をモデルへ作らせると、実在確認を通らない「引き継ぎWiki」「公式サイト」を
  出典名として生成できる。出典カードをサーバーだけで組み立てれば、この経路を閉じられる
- 外部CIで完全自動化すると、非公開Wikiの認証情報と取得データを外部へ預ける判断が必要になる

### 判断を変えた点

- フィードバックはFirestore保存後にSMTP通知し、通知結果を`sent / failed / disabled`で画面へ返す
- SMTPの宛先と認証情報は環境変数へ置き、コードと通知ログへ実アドレスを残さない
- 指示語の対象が直前の会話から一つに決まる場合は、言い換えを求めず直接答えるよう明記する
- Wikiだけを指定した質問では公式サイトの候補を除外し、URL質問ではリンクを含む本文を直接照合する
- モデルには出典一覧を作らせず、本文内で必要な実在URLだけを使わせる
- 更新検知と複数コマンドは自動化するが、本番反映は人が差分を確認してから行う
- Firestore導入前の`sessionStorage`履歴移行と旧利用回数Cookie移行は、初期運用前の役目を終えたため削除する

---

## M20. 最新バックエンドの本番反映

- 日付: 2026-08-09
- きっかけ: GitHubの`main`は更新済みだったが、Cloud Runは古いイメージのままで、
  フィードバック通知・会話参照・リンク検索の修正が本番APIへ入っていなかった
- 再現コマンド:

```bash
gcloud meta list-files-for-upload

docker buildx build --platform linux/amd64 --load -t IMAGE:b693a72 .
docker push IMAGE:b693a72
gcloud run deploy wasa-chat-api --image IMAGE:b693a72 \
  --region asia-northeast1 --platform managed --quiet

curl -fsS https://本番API/health
gcloud run services describe wasa-chat-api --region asia-northeast1 \
  --format='value(status.latestReadyRevisionName,spec.template.spec.containers[0].image)'

curl -sS -o /tmp/wasa-chat-production.js https://本番フロント/assets/index-現在値.js
rg -o 'メールで送信しました|メール通知に失敗しました|匿名で管理者にフィードバックを送れます' \
  /tmp/wasa-chat-production.js
```

### 数字

| 項目 | 結果 |
|---|---:|
| Cloud Buildへの送信候補 | **92ファイル / 4.2MiB** |
| 許可した非公開生成物 | **2ファイル**（`index.json` / `toc.md`） |
| Dockerビルドコンテキスト | **801.41kB** |
| 本番イメージタグ | **b693a72** |
| Cloud Runリビジョン | **00010-zq9** |
| 本番トラフィック | **100%** |
| ヘルスチェック | **HTTP 200 / 614ページ / 911チャンク** |
| 本番フロントの新フィードバック文言 | **3/3確認** |
| SMTP用シークレット | **0件** |

### 読み取れたこと

- `.gcloudignore`で`.env`、Wiki取得元、評価データを除外しつつ、本番に必要な索引2ファイルだけを送信候補へ残せた
- Cloud Build APIは有効化できたが、現在の利用者にはビルド作成権限がなく、API有効化だけでは実行できなかった
- Docker Desktopを起動する経路へ切り替え、同じDockerfileから`linux/amd64`イメージを作成・配備できた
- 最新リビジョンの`/health`は正常で、索引規模もローカル測定の614ページ・911チャンクと一致した
- Cloudflare Pagesにも、メール送信成功・通知失敗・匿名報告の新しい3文言が反映された
- SMTP用シークレットが無いため、フィードバックはFirestoreへ保存されるがメール通知はまだ無効である

### 判断を変えた点

- Docker Desktopを使えないときのCloud Build経路は残すが、実行権限が必要であることをデプロイ文書へ明記する
- 権限が無いときは迂回せず、認証済みのArtifact RegistryへローカルDockerからpushする
- メール通知は、GmailのアプリパスワードをSecret Managerへ登録してから一度に有効化する。通常のログインパスワードは使わない

---

## M21. Wiki添付画像の必要性 / 画像キャプション抽出の欠陥

- 日付: 2026-08-09
- きっかけ: 「画像の必要性を測る」（優先度B）。67枚から回路図・設計図を確認し、
  本文だけでは回答不能な質問が実在するかを、画像処理を足す前に確かめる
- 再現コマンド:

```bash
# 実データが使っている画像パラメータの全数
python3 -c 'import json,re,collections; t=collections.Counter()
for line in open("dump/pages.jsonl"):
    for m in re.finditer(r"\[\[\s*(?:ファイル|File|画像|Image)\s*:([^\[\]]*)\]\]", json.loads(line)["wikitext"], re.I):
        for p in [p.strip() for p in m.group(1).split("|")][1:]: t[p]+=1
print(t.most_common(8))'

# 本文の［図: …］が表示パラメータになっていないか
python3 -c 'import json,re; idx=json.load(open("data/index.json"))
f=[m for p in idx["pages"] for c in p["chunks"] for m in re.findall(r"［図: ([^］]*)］", c["text"])]
J=re.compile(r"^(サムネイル|中央|なし|フレームなし|\d*[x×]?\d+\s*(?:ピクセル|px))$")
print(len(f), sum(1 for x in f if J.match(x)), sum(1 for x in f if re.search(r"\.(png|jpg|jpeg)$", x, re.I)))'

python build_index.py && python build_toc.py
python eval/retrieval_eval.py
```

### 数字

| 項目 | 変更前 | 変更後 |
|---|---:|---:|
| Wiki添付画像 | **67枚** | 67枚 |
| 本文から参照される画像 | **55枚 / 57箇所 / 14ページ** | 同左 |
| 本文から一度も参照されない画像 | **12枚** | 同左 |
| 本文の［図: …］が表示パラメータ | **31/57（54.4%）** | **0/57** |
| 本文の［図: …］がファイル名 | 8/57 | **39/57** |
| Evidence Recall@5 | 17/31（54.8%） | **17/31（54.8%）** |
| MRR | 0.364 | **0.364** |
| 索引本文 | 646,856字 | 647,146字 |
| 目次 | 26,451字 | **26,451字**（図は目次に載せていない） |

図が無いと本文が成立するかを、機械規則で分類した（図以外の本文が20字未満ならC、
「図を参照 / 下の写真 / 次のように」等があればB、それ以外はA）。

| 分類 | 件数 |
|---|---:|
| C 図が唯一の情報源 | **7件**（手で見直すと**15件**） |
| B 本文が図を名指ししている | **20件** |
| A 本文だけで完結している | **30件**（うち8件は実質C） |

ゴールデンデータ33問のうち、正解チャンクに図を含む設問は**0件**。

### 読み取れたこと

**1. 画像パラメータの上位はすべて日本語の別名だった。**

`サムネイル` 39 / `なし` 16 / `中央` 10 / `NNNxNNNピクセル` 7 / `フレームなし` 2。
`IMAGE_MODIFIERS` は英語名（`thumb` `center` `none` …）しか除外していなかったため、
これらが「キャプション」として採用され、本文の図の半分以上が
`［図: サムネイル］` `［図: 中央］` という無意味な文字列になっていた。
**しかも害はそれだけではない。** キャプションがあると見なされた結果、
本来出るはずだったファイル名まで押し出されていた。

| ページ | 変更前 | 変更後 |
|---|---|---|
| 電装班 | `［図: なし］` | `［図: EE-SX460-P1 ピン配置.png］` |
| 電装班 | `［図: サムネイル］`×4 | `コンタクト.png` `ハウジング.png` `トップ.png` `ケーブル完成図.png` |
| 空力設計(41st) | `［図: 300x300ピクセル］` | `［図: 三面図.png］` |

**2. 本文だけでは答えられない箇所は実在する。ただし局所的である。**

最も明確なのは「抗力から見る人力飛行機の機速の考察」で、抗力と必要パワーの
**導出式9枚がすべて画像**である。本文は「次のように分解させていきましょう」と書いて
図を置くだけで、式そのものは資料に文字として存在しない。結論
（誘導抗力は揚力係数の2乗に比例する等）は文章にあるので全滅ではない。

回路系では `EE-SX460-P1 ピン配置` が該当する。`###### ピン配置` 節の中身が図だけで、
本文は「VCCに5V, GNDにGNDをつなげれば動作する」しか書いていない。

「電装39代設計コンセプト・反省」の「コンセプト」節も図のみだった。これは M2a で
**「リード文が空のページ5件」として挙げたうちの1つ**であり、空だった理由が画像だった。

**3. それでも、いま画像処理を足す根拠にはならない。**

ゴールデンデータ33問の正解チャンクに図を含むものは0件で、実利用の誤答としても
報告が無い。**「図が必要な箇所が資料に存在する」ことと「利用者が実際に答えを
得られていない」ことは別**である。

### 判断を変えた点

- **画像処理（OCR・画像理解）は足さない。** 先に、費用ゼロで直せる
  キャプション抽出の欠陥を直した。31件が `サムネイル` から実際のファイル名になり、
  モデルは少なくとも「そこに何の図があるか」を答えられるようになった
- 検索精度に退行がないことを確認した（Evidence Recall@5・MRRとも一致）。
  **データの取り違えを直すだけの変更でも、必ず同じ指標で測ってから入れる**
- 追加するときの対象は67枚全部ではなく、**数式画像（`Expresssion` 系9枚 + `Math_1.png`）と
  ピン配置1枚**に限る。写真30件は本文が完結しており、処理しても得るものが薄い
- 着手の条件は実利用のフィードバックとする。「必要な情報がない」の👎に
  図が絡む例が出たときに限って再検討する

### 残る課題

- 分類の機械規則は「図以外の本文が20字未満ならC」としたため、本文が
  「2022年12月時点」しかない『原状確認の写真』8件をAに数えた。
  **規則を実データに合わせて調整せず**、手で見直した件数を併記している
- 未参照の12枚（ロゴ、人物写真、見積書、`40th スケジュール0725.png` など）は
  索引に一切現れない。スケジュール画像は内容を持つが、本文が参照していないため
  今回の分類対象外である

---

## M22. キーボード操作とスクリーンリーダーの検査

- 日付: 2026-08-09
- きっかけ: 「キーボードだけで操作できるか」「ラベルと送信結果を読み上げで確認する」（優先度C）
- 再現コマンド:

```bash
grep -oh 'aria-[a-z]*' web/src/*.tsx | sort | uniq -c | sort -rn
grep -oh 'role="[a-z]*"' web/src/*.tsx | sort | uniq -c

cd web && npm run build
cd ../backend && GOCACHE=/private/tmp/wasa-go-cache go test ./... -count=1
```

### 数字

| 項目 | 変更前 | 変更後 |
|---|---:|---:|
| 常設のライブリージョン | **0** | **1** |
| ポップオーバーを閉じた後にフォーカスが戻る箇所 | **0/4** | **4/4** |
| 実装のない `role="menu"` / `role="menuitem"` | **5** | **0** |
| 役割のない要素に付いた `aria-label` | **1** | **0** |
| 同じ語を二度読み上げる箇所 | **1** | **0** |
| 初回JavaScript | 243.78kB（gzip 76.56kB） | **244.11kB**（gzip 76.62kB） |
| Go全パッケージ | — | **9/9 PASS** |

キーボードだけで到達・操作できることをコード上で確認した対象:

| 対象 | 結果 |
|---|---|
| 改善を送る / お知らせ / プロフィールの切り替え | Tabで到達、Enter・Spaceで開閉、Escapeで閉じる |
| フィードバックの分類・補足・送信・閉じる | すべて `button` と `textarea` で、分類は `aria-pressed` を持つ |
| アシスタントの並べ替え | `SelectMenu` が矢印・Home・End・Enter・Escapeに対応済み |
| アシスタントの作成 | `button` から `form` へ入り、送信まで到達できる |

### 読み取れたこと

**1. 送信結果は読み上げられていなかった可能性が高い。**

トーストは `{toast && <span role="status">…</span>}` の形で、**中身と一緒に
DOMへ現れていた**。ライブリージョンは「先に存在していて、後から中身が変わる」
ときにしか読み上げられないため、この形では通知されない。`key={toast}` で
毎回作り直していたので、なおさら条件を満たしていなかった。
常設の隠しライブリージョンを置き、見えるトースト側は二重読み上げを避けて隠した。

**2. Escapeで閉じるとフォーカスが `body` へ落ちていた。**

閉じる操作自体は動いていたが、開いた元のボタンへ戻していなかった。
キーボードだけの利用者は、閉じるたびにタブ順の最初からやり直しになる。
「閉じられるか」だけを見ると見落とす種類の不足だった。

**3. `role="menu"` は、実装していない操作方法を約束していた。**

履歴メニューは `role="menu"` / `role="menuitem"` を付けていたが、矢印キーでの
移動は実装していない。この役割を読み取ったスクリーンリーダーは矢印キーでの
移動を前提にするため、**役割を名乗るほど操作しにくくなる**。中身は素の
`button` でTabで辿れるので、`role="group"` と操作対象名のラベルへ変えた。

### 判断を変えた点

- ライブリージョンは常設し、文字だけを差し替える
- ポップオーバーとメニューは、閉じたら開いた元のボタンへフォーカスを戻す
- **実装していない操作方法を約束するARIAの役割は名乗らない。**
  `role` は見た目の分類ではなく、キーボード操作の契約として扱う
- 役割のない要素の `aria-label` は読み上げられないため、隠し文字でボタン名に足す

### 残る課題

- **実機のスクリーンリーダーでは未検証。** ここで直したのはコードから判断できる
  不足であり、VoiceOver・NVDAでの読み上げ順や日本語の読みは確認していない
- `main.conversation` に `aria-live="polite"` が付いており、SSEで1文字ずつ届く
  回答が繰り返し読み上げられる恐れがある。**実機で確認するまで変更しない**
  （直したつもりで別の不便を作る側の変更になるため）
- 名前変更ダイアログをEscapeで閉じたときの戻り先は、元のメニューごと消えるため
  未対応。実利用で困った報告が出てから決める
